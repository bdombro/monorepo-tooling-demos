{
  "name": "@mydomain/lib",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "echo yarn v$(yarn -v) && sleep 4 && tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "watch": "tsc --watch"
  },
  "license": "ISC",
  "devDependencies": {
    "lodash": "4.10.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "lodash": "^4.0.0"
  }
}
