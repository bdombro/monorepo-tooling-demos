{
  "name": "rush",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "bootstrap": "rush update",
    "bootstrap:with-up:": "rush install -t '@mydomain/app'",
    "build": "rush build",
    "build:scoped": "rush build -t '@mydomain/app'",
    "build:changed": "pnpm --filter '[<since>]' build",
    "clean": "pnpm --filter '*' clean",
    "exec": "pnpm --filter '*' exec pwd",
    "lint": "pnpm --filter '*' lint",
    "test": "pnpm --filter '*' test",
    "watch": "pnpm --filter '*' --parallel watch"
  }
}
